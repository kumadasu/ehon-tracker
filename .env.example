# Google OAuth Client ID (from Google Cloud Console)
# Required for Drive backup/sync and Google Calendar integration.
# Leave empty to run in localStorage-only mode (no sign-in).
VITE_GOOGLE_CLIENT_ID=
